# 小说内容规则

这份文件是小说内容的唯一格式规范。后续新增或修改小说时，必须遵守以下规则。

## 一、内容目录

小说内容必须放在 `content/novels` 目录中，不要放入旧的 `content/posts` 目录。

目录结构必须是：

```text
content/
└── novels/
    ├── 小说英文标识.mdx
    └── 小说英文标识/
        └── chapters/
            ├── 001_中文章节标题.mdx
            ├── 002_中文章节标题.mdx
            └── 003_中文章节标题.mdx
```

当前示例：

```text
content/novels/wuchenglaixin.mdx
content/novels/wuchenglaixin/chapters/001_雨夜旧札.mdx
```

小说文件名和章节目录名必须完全一致。小说文件名和目录名只使用小写英文字母、数字和短横线；章节文件名使用“三位数字 + 下划线 + 中文章节标题”的格式。

## 二、小说文件格式

每部小说必须有一个位于 `content/novels/*.mdx` 根目录的小说文件。

Frontmatter 必须包含以下字段：

```md
---
title: 雾城来信
description: 一封迟到了十年的信，把年轻的邮差带回一座被雾笼罩的旧城。
status: 连载中
---
```

字段规则：

| 字段 | 必填 | 规则 |
| --- | --- | --- |
| `title` | 是 | 小说正式名称，使用中文 |
| `description` | 否 | 一句话简介，使用中文 |
| `status` | 是 | 只能填写 `连载中` 或 `已完结` |

小说正文可以填写作品简介，但章节正文不写在小说文件中。

## 三、章节文件格式

每一章必须放在对应小说的 `chapters` 目录下。

Frontmatter 必须包含以下字段：

```md
---
title: 雨夜来信
chapterNumber: 1
---
```

字段规则：

| 字段 | 必填 | 规则 |
| --- | --- | --- |
| `title` | 是 | 章节标题，不要重复写“第几章” |
| `chapterNumber` | 是 | 正整数；同一部小说中不能重复 |

章节正文写在 Frontmatter 结束标记 `---` 的下方，使用 Markdown 或 MDX。

示例：

```md
---
title: 雨夜来信
chapterNumber: 1
---

雨从傍晚一直下到深夜。

林舟在打烊前收到一封没有寄件人地址的信。
```

## 四、章节排序和链接

1. 章节显示顺序只由 `chapterNumber` 决定。
2. 文件名前面的数字用于阅读和管理，必须使用三位数字，例如 `001`、`002`、`010`。
3. 章节文件名必须使用 `NNN_中文章节标题.mdx` 格式，例如 `001_雨夜旧札.mdx`；章节标题中的中文标点可以使用。
4. 不要手动填写 `slug`、`novelSlug` 或 `chapterSlug`，这些字段由 Contentlayer 自动生成。
5. 不要在章节文件中填写所属小说字段，所属小说由目录位置自动确定。

## 五、正文写作规则

1. 小说名称、章节标题和正文默认使用中文。
2. 使用自然段分隔正文，不要把整章内容写成一个超长段落。
3. 可以使用二级、三级标题，但正文标题不要替代章节 Frontmatter 的 `title`。
4. 图片使用 MDX 的 `Image` 组件，并且必须提供 `src`、`width`、`height` 和 `alt`。
5. 图片文件放在 `public` 目录中，`src` 使用以 `/` 开头的路径。
6. 不要在正文中直接使用外部脚本、任意 HTML 脚本或不可信组件。

## 六、给内容生成者的执行规则

当收到“新增小说”指令时：

1. 先创建小说文件 `content/novels/<小说英文标识>.mdx`。
2. 再创建对应的章节目录 `content/novels/<小说英文标识>/chapters/`。
3. 每一章创建独立的 MDX 文件，文件名使用 `NNN_中文章节标题.mdx`。
4. 确认所有章节的 `chapterNumber` 唯一且连续或符合用户指定的编号。
5. 确认 `status` 只能是 `连载中` 或 `已完结`。
6. 不要修改页面路由来添加普通小说内容。

当收到“新增章节”指令时：

1. 找到目标小说对应的章节目录。
2. 使用下一个未使用的三位数字编号。
3. 使用 `NNN_中文章节标题.mdx` 创建章节文件，只新增章节文件，不新增小说文件。
4. 保持章节标题和正文为中文。

当收到“小说完结”指令时，只把对应小说文件中的：

```yaml
status: 连载中
```

改为：

```yaml
status: 已完结
```

## 七、禁止事项

- 不要把小说写成旧博客文章格式。
- 不要把多个章节写在同一个章节文件中。
- 不要把章节正文写进小说文件，除非用户明确要求作品简介。
- 不要重复使用同一部小说的章节编号。
- 不要使用未定义的小说状态。
- 不要把新小说放进 `content/posts`。
